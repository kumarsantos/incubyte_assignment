import StringCalculatorApp from "./components/StringCalculatorApp";

const App = () => {
  return (
    <main
      style={{
        padding: "20px",
        backgroundColor: "#fff",
        color: "#222",
        fontFamily: "system-ui, sans-serif",
        maxWidth: "600px",
        margin: "auto",
      }}
    >
      <StringCalculatorApp />
    </main>
  );
};

export default App;
